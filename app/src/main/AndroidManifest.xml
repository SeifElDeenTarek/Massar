<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DarkAppTheme">
        <activity
            android:name=".ui.home.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/DarkAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.courses.CoursesActivity"
            android:label="@string/home_courses"
            android:parentActivityName=".ui.home.HomeActivity" />
        <activity
            android:name=".ui.testYS.TestYourselfActivity"
            android:label="@string/home_test_yourself"
            android:parentActivityName=".ui.home.HomeActivity" />
        <activity
            android:name=".ui.examPrep.ExamPrepActivity"
            android:label="@string/home_exam_prep"
            android:parentActivityName=".ui.home.HomeActivity" />
        <activity
            android:name=".ui.maqal.MaqalActivity"
            android:label="@string/home_maqal"
            android:parentActivityName=".ui.home.HomeActivity" />
        <activity
            android:name=".ui.PdfActivity" />
        <activity
            android:name=".ui.explain.ExplainActivity" />
        <activity
            android:name=".ui.test.TestActivity" />
    </application>

</manifest>